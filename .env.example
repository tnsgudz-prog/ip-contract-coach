# Environment
.env                # 로컬 환경변수(값 포함)
.env.local          # 로컬 오버라이드(값 포함)
.env.*              # 기타 로컬 환경 파일

# 복사본 생성 시 사용: 실제 값은 넣지 말고 아래 파일명을 참고해 직접 생성
# .env.example      # 템플릿(값 없음, 키 이름만)
